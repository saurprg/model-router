# Gateway auth (client → your router)
GATEWAY_API_KEY=dev-gateway-key-change-me

# DigitalOcean Gradient serverless inference (OpenAI-compatible gateway)
DO_MODEL_ACCESS_KEY=

# Native provider keys (optional — only if calling api.openai.com / api.anthropic.com directly)
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# Groq native API
GROQ_API_KEY=
